# Frontend

백엔드가 서빙하는 정적 화면이다.
프론트엔드는 `/health`, `/users`, `/groups` 등 절대 경로로 API를 호출하므로
`index.html`을 파일로 직접 열면 동작하지 않는다.

## 사용 방법

1. 백엔드를 먼저 실행한다.
2. 브라우저에서 `http://localhost:3000`으로 접속한다.
3. 사용자/스터디 생성, 참여, 목록 조회를 테스트한다.
